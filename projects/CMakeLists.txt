# Placeholder — add project subdirectories here.
# Any executable/library that should use all dependencies:
#   target_link_libraries(my_target PRIVATE Nest_AllDeps)
#
# If you only need a subset, link against specific targets instead.